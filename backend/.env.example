# Mirror Backend Environment Configuration
# Copy this file to .env and update values

# Server
PORT=5000
NODE_ENV=development

# CORS
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5000

# Database (when migrating from file storage)
# DATABASE_URL=

# Logging
LOG_LEVEL=info

# Email (for future The Veil email feature)
# SMTP_HOST=
# SMTP_PORT=
# SMTP_USER=
# SMTP_PASS=
# FROM_EMAIL=
